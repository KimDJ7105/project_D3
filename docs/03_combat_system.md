---
title: 전투 시스템 기획
status: 핵심 규칙 결정됨, 수치 밸런스 미정
last_updated: 2026-08-18
---

# 전투 시스템

## 형식
2D 탑뷰(2.5D 연출), **자유 이동 기반** 턴제 전술 전투. 원래는 칸(그리드) 기반으로 시작했으나 2026-08-18에 자유 이동으로 전환 — 아래 "전투 중 이동과 스테미나" 참고.

## 비주얼/연출 레퍼런스
건담 G 이터널의 전투 화면과 유사한 구도를 목표로 함 (탑뷰 위 유닛 배치, 턴제 진행). 다만 그리드를 버리고 자유 이동+스테미나 기반으로 가면서 순수 그리드 전술물보다는 액션-전술 하이브리드에 가까운 느낌으로 옮겨감 — 애초에 그리드는 별 고민 없이 정한 초기값이었을 뿐 지켜야 할 취향적 이유는 없었다고 확인함(2026-08-18).

## 탐사 ↔ 전투 전환 (결정됨 — 2026-08-09, 전투 방식 2026-08-18 갱신)
던전 탐사는 자유 이동(8방향)으로 진행하다가, 전투 진입 시 턴제 전투로 전환된다. 탐사와 전투 모두 자유 이동이라는 점은 같지만, 전투 중에는 턴 순서·스테미나 제한이 걸린다(아래 참고).
- **진입 트리거**: 필드 조우식(시야 접촉). 탐사 중 적이 필드에 보이고 플레이어(또는 적)의 시야에 포착되면 전투 개시.
- **전환 방식**: 심리스. 별도 로딩/화면 전환 없이, 현재 필드에서 보이던 그 영역이 그대로 전투 공간으로 재구성된다 (크로노 트리거류).
- 이 구조는 [[02_dungeon_town_structure]] 의 마을-던전 루프, 던전 내 랜덤/네임드 NPC 조우([[01_relationship_system]])와 직접 연결됨.

## 파티 규모 (결정됨 — 2026-08-09)
- 플레이어 파티(플레이어 + 동료 NPC) 기준, 초반은 소규모(2~3명), 중반부터는 중규모(4~6명)로 커지는 성장 곡선.
- 솔로 플레이도 가능해야 함 — 파티원 없이 진행하는 자체 하드모드 성격.
- 몬스터 측 유닛 수는 플레이어 파티보다 많을 수 있음(비대칭 인원수 허용).

## 전투 중 이동과 스테미나 (그리드 폐기, 확정 — 2026-08-18)
기존엔 전투를 칸(그리드) 기반으로 진행할 계획이었으나, **자유 이동(연속 좌표) 기반으로 전환**하기로 확정. 전환 사유:
- 스킬 시스템([[06_skill_style_system]])도 던전 절차적 생성([[02_dungeon_town_structure]])도 아직 코드가 하나도 없는 시점 — 둘 다 그리드를 전제로 먼저 만들면 나중에 다시 만들어야 했을 것. 지금이 전환 비용이 가장 낮은 시점.
- 클릭 이동에 경로탐색(장애물 회피)이 필요한 건 그리드였어도 마찬가지였을 것(칸 A→B 이동도 결국 길찾기가 필요) — 그리드를 유지해서 얻는 이득이 생각보다 적었음.
- 엄폐/시야를 실제 3D 지오메트리에 레이캐스트로 판정하는 게 그리드 셀 플래그로 관리하는 것보다 자연스러움.

**이동 방식**: 전투 중에도 그리드 스냅 없이 클릭한 지점으로 이동(2026-09-16 구현, **2026-09-19에 순간이동 → 경로를 따라 걷기로 교체**). 처음엔 위치를 바로 덮어쓰는 방식이라 벽이 없는 씬에선 티가 안 났는데, 1층에 벽이 생기자 벽을 그냥 통과하는 문제가 드러났음. 지금 규칙(사용자가 직접 정한 것):
- 클릭하면 **내비메시 경로를 따라 걸어감**(벽/기둥을 돌아서). 클릭 지점은 가장 가까운 걸을 수 있는 지점으로 보정되므로 벽을 눌러도 벽 앞까지 걸어감.
- **경로가 연결되지 않으면 아예 이동하지 않음**(스테미나도 안 씀).
- 스테미나가 부족한 곳을 눌러도 **스테미나가 허용하는 지점까지만** 걷고 멈춤. 스테미나는 직선거리가 아니라 **실제로 걸은 거리**만큼 걸으면서 차감 — 그래서 돌아가는 길은 그만큼 더 듦.
- 걷는 동안 **다른 입력은 막힘**(스킬 장전, 턴 종료, 되돌리기, 투척). 대신 **클릭하면 그 자리에서 멈출 수 있음**(걷는 걸 보다가 스테미나를 보고 멈추기 위함, 걸은 만큼만 스테미나 소모). 멈추는 클릭은 새 이동/스킬을 시작하지 않음.
- **이동 가능 영역 표시는 만들지 않기로 함**(사용자 판단). 현재 이동 반경 원반(`MovementRangeIndicator`)은 벽으로 돌아가야 하는 곳이 있으면 부정확하지만, 지금은 그대로 둠.
- 내비메시는 층 지오메트리를 세운 직후 한 번 구움(`DungeonDressing._bake_navigation()`, 레이아웃이 고정이라 매번 같은 결과). 레이어 1 정적 충돌체만 반영(적의 클릭용 충돌체는 제외).
- 알려진 한계: 벽 윗면이 내비메시에 걸어갈 수 있는 섬으로 잡힐 수 있음(바닥과 연결되지 않아 경로엔 영향 없음). 되돌리기(R)는 여전히 즉시 원위치로 순간이동. 등반 스테미나 추가 소모 미구현. 얇은 벽은 양쪽 면에서 같은 거리인 지점을 누르면 반대편으로 돌아가서 도착할 수 있음.

**이동 가능 범위**는 새로 추가된 **스테미나** 스탯으로 제한한다(아래 스탯 체계 참고):
- 전투 중 이동 거리에 비례해 스테미나 소모, 고지대로 오를수록 추가 소모(기존 "등반 시 이동력 2배 소모" 아이디어를 스테미나 소모량 증가로 대체).
- 공격/스킬 사용 시에도 스테미나 소모(스킬마다 비용이 다름).
- **매 턴 시작 시 최대치로 전량 리필**(이월 없음) — 규칙을 단순하게 유지.
- 목적: 이동과 행동을 같은 자원으로 묶어서, 원거리 캐릭터가 "이동→공격→이동→공격"을 매턴 무한 반복(카이팅)하는 걸 자연스럽게 억제하고, 매턴 "얼마나 움직이고 뭘 쓸지"를 전략적으로 고민하게 만듦.
- 이동/스킬별 정확한 소모량, 등반 배율은 다른 수치들처럼 밸런싱 영역 — 미정, 구현 시 별도 함수/테이블로 분리.
- **탐사 중 자유 이동(WASD)에는 스테미나 소모 없음** — 전투 중에만 적용되는 자원.

**턴 구조 변경**: 기존엔 "행동 1회 = 턴 종료"로 단순했는데, 스테미나가 허용하는 한 이동과 행동을 여러 번 섞어 쓸 수 있어야 하므로 **플레이어가 직접 "턴 종료"를 선택해야 턴이 끝나는 구조**로 변경 확정, 구현 완료(`end_turn` 입력 액션, 2026-09-16) — 스테미나가 0이 돼도 자동으로 턴이 끝나지는 않음(수동 종료만 인정, 아래 열린 질문에서 재검토 여지는 남김).

**이동 되돌리기 (확정 및 구현 완료 — 2026-09-16)**: 그 턴에 공격/투척을 아직 안 했다면, 이동(스테미나 소모 포함)을 몇 번 했든 **턴 시작 시점의 위치/스테미나로 전부 되돌릴 수 있음**(단계적 undo 아님 — 한 번에 턴 시작 상태로 리셋). 대신 **공격이든 투척이든 뭔가를 실행하는 순간부터는 그 턴이 끝날 때까지 되돌리기가 완전히 잠김** — 공격까지 무를 수 있으면 전략적 이점이 너무 커진다고 판단(예: 찌르고 결과가 나쁘면 되돌리는 식의 무손실 탐색을 막기 위함). "이동→공격→이동"처럼 공격 후에 또 움직이는 것 자체는 여전히 가능하지만, 그 이후로는 되돌리기를 못 씀. `undo_movement` 입력 액션(R 키)으로 구현.

## 턴 순서 (결정됨 — 2026-08-09, 속도 산출 방식 2026-08-13)
개별 유닛의 속도 스탯 기반 우선순위 큐(초리어티 큐, FF택틱스/슬레이 더 스파이어류). 아군/적군 유닛이 속도 순으로 뒤섞여 행동.

전투에 쓰이는 실질 속도는 단일 스탯이 아니라 합성값:
```
실질 속도 = 기본 속도 스탯 + 기량 구간별 보너스(계단식) + 장비 보정
```
- **기량 구간별 보너스**는 기량 수치를 구간(예: 1~10, 11~20 ...)으로 나눠 구간마다 다른 보너스를 더하는 계단식 — 정확한 구간 경계/증가폭은 밸런싱 영역이라 미정, 구현 시 별도 테이블/함수로 분리해 나중에 숫자만 조정 가능하게 함.
- 이 구조 때문에 **속도가 7번째 핵심 스탯으로 확정**됨 (아래 스탯 체계 참고).

## 사거리/시야 — 엄폐 및 고저차 (결정됨 — 2026-08-09, 이동 관련 수치는 2026-08-18 스테미나로 대체)
- **엄폐(커버)**: 구현. 장애물이 시야/공격을 막음 — 그리드 폐기로 셀 플래그 대신 실제 3D 지오메트리 레이캐스트 기반으로 판정할 전망(세부는 열린 질문).
- **고저차**: 단순화된 단계형(1단/2단/3단 등)으로 구현. 복잡한 실시간 높이 계산은 지양.
  - 더 높은 곳에서 원거리 공격 시 사거리 또는 명중률에 보너스.
  - 높은 곳으로 이동(등반)할 때는 ~~이동력을 2배로 소모~~ **스테미나를 추가로 소모**(정확한 배율은 미정 — "전투 중 이동과 스테미나" 참고).
- 1인 개발 부담이 과도하다고 판단되면 고저차는 제거하고 엄폐(장애물)만 남기는 것도 대안으로 열어둠.

### 고저차 사거리 보정과 사거리 영역 표시 (방향 확정, 미구현 — 2026-09-19)
지금 구현은 XZ 평면 거리만 보고 **높이에 따른 보정은 아직 전혀 없음**(높낮이 있는 지형 자체가 없어서). 앞으로 넣을 방향:
- **보정 대상은 사거리로 확정**(위 "사거리 또는 명중률" 중 사거리 쪽 — 사용자가 "높은 곳에서 사거리 부스트"를 원한다고 명시). 기준 거리는 계속 XZ 평면 거리이고, 높이 차는 **유효 사거리 보정**으로만 반영: `유효 사거리 ≈ 스킬 사거리 + (내 높이 − 대상 높이) × 계수`. 내가 더 높으면 늘고, 대상이 더 높으면 줄고, 같은 높이면 보정 없음(한 식으로 양방향 처리). 근접 스킬은 높이 차가 너무 크면 아예 닿지 않게 하는 상한이 필요할 수 있음. 계수는 밸런싱 영역.
- **표시 방식 확정: 지형을 따라가는 사거리 영역.** 사거리를 원반 하나로 그리는 대신, "그 바닥 지점에 서 있는 적을 내가 때릴 수 있는가"를 지점마다 계산해서 **때릴 수 있는 곳만 칠한 영역**을 보여줌 — 낮은 쪽은 불룩하게 넓어지고 높은 쪽은 움푹 좁아짐. 이유: (1) 적 위에 마우스를 올려야 숫자로 알려주는 방식은 "고지대에서 이 적을 때릴 수 있다는 걸 모르는" 문제와 "아래서 고지대 적을 노리는데 왜 안 되는지 모르는" 문제를 못 풀고, (2) 원반 안팎에 점선 링을 여러 개 두는 방식은 지저분해 보임. 영역 자체가 곧 답이라 표시와 판정이 같은 계산을 쓰므로 어긋날 일이 없음. 광역기의 바닥 조준에도 그대로 쓸 수 있음.
- **엄폐도 같은 영역에서 처리 가능**: 벽에 가려 못 때리는 지점을 영역에서 빼면 됨 — 엄폐 세부 규칙(아래 열린 질문)을 정할 때 이 표시를 전제로 함.
- **부드러운 경계**: 영역 경계가 계단처럼 꺾이는 건 지형 높이를 3단계 정수 값 그대로 쓸 때 생기는 것 — 높이를 내부적으로 **연속값(float)**으로 다루고 단계(1/2/3)는 레벨 디자인용 눈금으로만 쓰면(경사로/계단 구간은 사이 값을 가짐) 경계가 자연스럽게 이어짐. 진짜 절벽 경계는 실제로도 사거리가 갑자기 달라지므로 판정은 그대로 정확하게 두고, 표시만 좁은 폭으로 페이드해서 보기 좋게 함 — 페이드를 넓게 잡아 표시가 판정과 모순되어 보이지 않게 주의.
- 구현 시점: 1층에 실제 높낮이를 넣을 때(높이 데이터가 있어야 그리고 테스트할 수 있음). 그때 클릭 → 바닥 지점 변환도 "플레이어 높이의 평평한 평면" 가정에서 실제 바닥 지오메트리 광선으로 바꿔야 함.

## 지형 효과 (결정됨 — 2026-08-09, 그리드 폐기로 저장 방식 갱신 — 2026-08-18)
~~그리드 칸(타일) 단위로 상태를 저장할 수 있는 구조를 전제로~~ 그리드가 없어졌으므로 특정 위치를 중심으로 한 영역(반경/모양)에 상태를 부여하는 구조로 변경(그리드 폐기와 연동, 세부 구현 방식은 미정):
- **이동 방해 지형**: 스테미나 추가 소모 등.
- **상태이상 지형**: 특정 영역에 있으면 상태이상 부여(예: 화상/독 장판).
- **장판 조작**: 아이템으로 특정 영역의 지형 상태를 일시적으로 변경 가능(예: 불 장판을 물로 꺼서 무력화 등).

## 스탯 체계 (핵심 8개 확정 — 2026-08-10 6개 확정, 2026-08-13 속도 추가, 2026-08-18 스테미나 추가)
다크소울류를 계승한 스탯 체계, 핵심 8개로 확정: **체력 / 힘 / 기량 / 지능 / 신앙 / 신비 / 속도 / 스테미나**.
- 체력/힘/기량 — 물리 계열 스킬([[06_skill_style_system]] 참고)의 주 스케일링 대상.
- 지능/신앙 — 기존 마법류 스킬의 주 스케일링 대상. 유파에 따라 지능 위주 vs 지능+신앙 혼합 등 비중이 다름.
- 신비 — 던전 기원 신마법류 스킬에서 더 자주 쓰이는 경향(전용은 아님, 유파별로 비중 다름).
- **속도** — 초리어티 큐(턴 순서)의 기반 스탯. 위 "턴 순서" 섹션 참고, 기량과 장비의 영향을 받아 실질 속도로 합성됨. 턴 순서 결정에만 관여 — 아래 스테미나와 역할이 분리됨.
- **스테미나** — 전투 중 "내 턴에 뭘 할 수 있는지"(이동 범위 + 행동 가능 여부)를 결정하는 자원. 위 "전투 중 이동과 스테미나" 참고. 속도와 별개의 독립적인 핵심 스탯으로 확정 — 속도에 연동시키면 빠른 캐릭터의 가치가 턴 순서+행동력 두 배로 뻥튀기되는 문제가 있어 분리를 선택함.
- 9개 이상으로 추가 확장할지는 미정.

## 관계 시스템과의 연결
전투 자체의 규칙과는 별개로, 관계 시스템이 전투 발생/진행에 개입하는 지점이 있음:
- 관계가 나쁜 세력과의 조우 → 의도치 않은 전투 발생.
- 관계가 좋은 NPC의 난입 지원 → 위기 상황에서 전투 국면 전환.
- 상세 트리거 및 확률은 [[01_relationship_system]] 에서 별도 설계 필요.

## 스킬 시스템
전투 스킬은 [[06_skill_style_system]] 의 유파(스킬 계열) 시스템을 따른다 — 공용 스킬 + 유파 전속 스킬, 던전 진입 전 마을에서 고정하는 스킬 슬롯(기본안 4개) 구조. **핵심 실행 구조(데미지/사거리/효과)는 유파/슬롯 없이 v1으로 구현 완료 — 2026-09-17**, 아래 참고 및 [[06_skill_style_system]] "구현 v1".

**스킬 범위 구조 (확정 — 2026-08-18, 투척에 구현 완료 — 2026-09-16, 근접 공격도 클릭 조준으로 구현 완료 — 2026-09-17)**: 그리드 폐기에 맞춰 스킬의 범위를 **사거리(시전자 → 목표 지점까지의 최대 거리) + 효과 반경(그 지점을 중심으로 영향을 받는 범위)**의 이원 구조로 통일. 효과 반경이 0이면 단일 타겟 즉시타격, 0보다 크면 광역기가 되는 식으로 하나의 구조에서 양쪽을 다 표현. 판정은 **XZ 평면 거리 기준**(고저차 Y는 무시) — `Targeting.gd`(구 `Grid.gd`)가 이미 하던 평면 가정을 그대로 이어감, 고저차는 위 "사거리/시야" 섹션의 사거리·명중 보너스로만 별도 취급. 뼛조각 투척이 이 구조로 구현됨 — `ItemData.throw_range`(사거리, 범위 밖이면 던지기 자체가 거부)+`effect_radius`(효과반경, 범위 안인데 빗나가면 아이템/스테미나는 그래도 소모).

**스킬 데미지 공식 구현 (확정 — 2026-09-17)**: 기존 `CombatFormulas.basic_attack_damage()`(힘 스탯 고정 사용 placeholder)를 폐기하고 `SkillData` 리소스 + `CombatFormulas.skill_damage()`로 교체 — 데미지 = `scaling_stat`(스킬이 쓰는 스탯 이름, `Stats.gd` 필드와 일치) × `scaling_multiplier`(등급 체계가 아직 없어서 쓰는 평평한 임시 배율). 플레이어는 스킬 2개("베기" E, "강타" 2번키)를 고정 장착(유파/슬롯 시스템 없이 하드코딩), 몬스터도 같은 구조로 `MonsterData.attack_skill` 하나를 가짐. 이걸로 지금까지 있던 "공격이 사거리/스탯 스케일링 없이 그냥 힘 스탯만 반영"하던 placeholder를 실제 스킬 시스템의 축소판으로 대체.

**공격도 클릭으로 조준 (확정 — 2026-09-17, 단일 타겟/광역기 판정 방식 정정 — 2026-09-17)**: 처음 스킬 시스템을 넣었을 때는 스킬 키를 누르면 곧바로 유일한 적에게 자동 적용되게 구현했는데, 사용자가 실제로 테스트해보고 "사거리/범위를 테스트하기 어렵다"고 지적 — 마우스로 "누구를/어디에" 조준하는 단계가 아예 없었던 게 문제였음. 그래서 스킬 키는 스킬을 "장전"만 하고(`Player.start_aiming_skill()`), 그다음 좌클릭으로 실제 목표 지점을 확정(`skill_aim_requested` 시그널 → `Dungeon._on_skill_aim_requested()`)하는 2단계 구조로 변경 — 이동 클릭과 스킬 조준 클릭을 `Player.pending_skill_index`로 구분. 우클릭이나 같은 스킬 키를 다시 누르면 조준 취소. 조준 지점이 사거리 밖이면 아예 거부(아무것도 소모 안 됨). `MovementRangeIndicator`가 조준 중엔 이동 반경 대신 **스킬 사거리 원반**(다른 색상)을 보여줘서 사거리를 눈으로 확인할 수 있게 함. 테스트용 사거리도 두 스킬을 다르게 잡음 — 베기 2.0(더 멀리서 견제), 강타 1.0(더 가까이 들어가야 하는 대가로 데미지 1.8배 — 리스크/리워드 느낌도 자연스럽게 생김).

**단일 타겟과 광역기는 판정 방식이 다름 (확정 — 2026-09-17)**: 처음엔 사거리 안에서 적과 먼 지점을 클릭하면 둘 다 "빗나감"(스테미나만 소모)으로 통일했는데, 사용자가 명확히 기각 — 턴제 게임에서 단일 타겟 스킬에 마우스 정밀도를 요구하는 건 과함("FPS도 아닌데"). 재검토 후 **목적 자체가 다르다는 결론**으로 판정 방식을 분리:
- **단일 타겟**(효과반경 0): 실제 대상 선택이 필요함. 클릭이 살아있는 적에 직접 닿아야 하고(아래 "적 클릭 판정" 참고), 안 닿으면 **아무 일도 안 일어남, 아무것도 소모 안 됨** — "빗나감"이 아니라 "유효한 대상을 못 골랐음"으로 취급. 사거리는 클릭한 바닥 지점이 아니라 **선택된 적까지의 거리**로 잰다.
- **광역기**(효과반경 > 0): 대상 존재와 무관하게 항상 발동(스테미나 소모). 장판/함정 설치처럼 대상이 없어도 전략적으로 위치에 미리 깔아두는 용도가 있기 때문 — "실수 처벌용"이 아니라 "위치 지정형 스킬은 원래 그렇게 쓰는 것"이라는 취지.
**적 클릭 판정 (확정 — 2026-09-19)**: 처음엔 단일 타겟도 "클릭한 바닥 지점이 적 근처(반경 1.0)인가"로 판정했는데, 테스트해보니 벽 뒤 적을 클릭하면 적을 못 때렸음. 원인은 벽 클릭 판정이 아니라 조준 방식 자체 — 카메라가 25°로 비스듬히 내려다봐서 적 몸통(이나 그 앞을 가린 벽)을 클릭하면 광선이 적 **뒤쪽 바닥**에 닿아서 반경 밖으로 판정됨. 그래서 적에게 스프라이트 크기의 클릭용 충돌체(`Monster`의 pick body, 전용 물리 레이어 2)를 달고, 단일 타겟 조준은 **카메라 광선이 적 충돌체에 직접 맞는가**(`Targeting.pick_enemy()`)로 판정하도록 변경. 벽은 기본 레이어(1)라 이 광선에 안 걸려서 벽이 클릭을 가로챌 수 없고, 정확한 위치를 눌러야 하는 부담도 사라짐. 이동/광역기는 그대로 바닥 지점 사용. **알려진 부작용**: 엄폐/시야 판정이 아직 없어서(위 "사거리/시야" 참고) 사거리만 되면 벽 너머 적도 공격 가능 — 의도된 게 아니며, 엄폐를 어떻게 처리할지는 1층 구조 설계 때 함께 고민하기로 함.
- 이 구분 때문에 단일 타겟 스킬은 애초에 "빗나가서 손해 보는" 상황 자체가 없어짐(유효 대상 없으면 그냥 발동을 안 함) — 광역기만 "위치를 잘못 예측하면 손해"라는 텍스처를 가짐.

## 열린 질문 (미정)
- 스테미나 소모 공식(이동 거리당 소모량, 등반 배율, 스킬별 소모량)의 정확한 수치 — 밸런싱 영역. 지금은 `CombatFormulas.MOVE_STAMINA_COST_PER_UNIT`(이동)와 `SkillData.stamina_cost`(스킬별)/`THROW_STAMINA_COST`(투척)에 플레이스홀더 값만 있음, 등반 배율은 구현 자체가 아직 없음(고저차 있는 지형이 없어서).
- 스테미나가 0이 되면 자동으로 턴이 끝나는지 — 지금 구현은 항상 수동 "턴 종료"만으로 끝남(자동 종료 없음), 재검토 여지 있음.
- 경로탐색은 구현됨(2026-09-19, `NavigationServer3D` 직접 사용 — `NavigationAgent3D` 노드는 안 씀). 남은 것: 몬스터가 움직이게 될 때의 경로탐색, 고저차 등반 스테미나, 이동 범위 표시를 그대로 둘지/없앨지, 되돌리기를 걷기 애니메이션으로 할지. 층 레이아웃이 고정 변형이라 내비메시는 로드 시 한 번 굽는 걸로 충분(런타임 절차적 베이킹은 필요 없어짐).
- 이동 범위 표시(`MovementRangeIndicator`)에 정확한 클램프 지점 프리뷰(호버 마커)를 추가할지 — 지금은 반경 원반만 보여주고 스코프에서 뺌.
- 광역기(효과반경 > 0)가 실제로 생겼을 때, 조준점 기준 효과반경 프리뷰(호버 시 원 미리보기) 추가 — 지금 있는 스킬 둘 다 효과반경 0이라 아직 볼 수 있는 게 없어서 미착수.
- 엄폐(커버)의 구체적 판정 방식(레이캐스트 기반 세부 규칙, 부분 엄폐 여부 등) — 아직 구현 자체가 없어서 지금은 벽 너머 적도 사거리만 되면 때릴 수 있음(사용자가 처리 방식 고민 중, 2026-09-19). 결정되면 위 "고저차 사거리 보정과 사거리 영역 표시"의 영역에서 가려진 지점을 빼는 식으로 표시.
- 고저차 사거리 보정 식의 계수, 근접 스킬의 높이 차 상한, 지형 높이를 연속값으로 다룰지(경사로 등) 최종 확정 — 구현 시점(1층에 높낮이를 넣을 때)에 결정.
- 기량 구간별 속도 보너스의 정확한 구간 경계/증가폭 (밸런싱).
- 지형 상태(이동방해/상태이상) 종류 목록 및 장판 조작 아이템 체계 세부.

## 관련 문서
- [[00_game_overview]]
- [[01_relationship_system]]
- [[02_dungeon_town_structure]]
- [[06_skill_style_system]]
