---
title: 결정 로그
status: 진행 중
last_updated: 2026-08-09
---

# 결정 로그

새로운 결정이 생길 때마다 날짜순으로 아래에 추가한다.

## 2026-08-09
- 게임 컨셉 확정: 2D 탑뷰, 칸 기반 턴제 전술 전투 + 마을/던전 순환 구조 + 관계 시스템 중심 RPG.
- 핵심 축 2가지로 확정: (1) NPC/단체 관계 시스템, (2) 던전 탐사.
- 전투 비주얼 레퍼런스: 건담 G 이터널.
- 비주얼 스타일 지향점: 옥토패스 트래블러류 2.5D (다만 1인 개발 역량상 2D로 시작).
- 엔진 결정: **Godot 4** (이유는 [[04_tech_stack]] 참고). 개발자는 Godot 최초 사용, C++ 경험 있음.
- 프로젝트 문서 체계(`docs/`) 및 Godot 프로젝트 뼈대 생성.
- 던전 생성 방식 결정: **하이브리드** (구조/몬스터/함정 절차적 생성 + 관계 기반 NPC 조우는 별도 레이어). — [[02_dungeon_town_structure]]
- 던전 NPC를 랜덤 NPC(비네임드, 성향 기반)와 테이블/트리거형 NPC(네임드·소속 집단, 조건부 등장) 두 종류로 분류. — [[02_dungeon_town_structure]]
- 랜덤 NPC "승격" 메커니즘 채택: 플레이어와 유의미한 상호작용을 겪은 랜덤 NPC가 네임드/테이블-트리거 NPC로 전환되어 이후 재조우 가능 (네메시스 시스템 참고). — [[01_relationship_system]]
- 개인 특성(Trait) 시스템 컨셉 채택: 관계와 별개로 탐욕/이타심/공포내성 등 성격 수치를 NPC에 부여, 상황에 따른 배신/도주 등 돌발 행동에 사용. 세부 공식은 미정. — [[01_relationship_system]]
- 관계-특성 상호작용 확정: 돌발 행동 발생 확률은 특성 단독이 아니라 특성×관계 조합으로 결정 (관계가 좋으면 배신 확률 감소). — [[01_relationship_system]]
- 특성 가변성 확정: 네임드 NPC는 서사를 통해 특성이 변화(성장 아크 가능), 랜덤 NPC는 고정. — [[01_relationship_system]]
- 노출 방향 확정: 관계도는 플레이어에게 노출(정확한 수치 vs 등급 표시는 미정), 개인 특성은 기본 비공개 후 관계도가 매우 높아지거나 특정 아이템/스킬로 확인. — [[01_relationship_system]]
- 유파(스킬 계열) 시스템 컨셉 채택 (세키로 영감): 공용 스킬 + 유파 전속 스킬 이원화, 유파 중복 입문 가능(일부는 조건부), 숙련도로 상위 스킬 해금, 스킬 슬롯(기본안 4개)은 던전 진입 전 마을에서 고정하며 던전 내 변경 불가, 고위 기술은 하나의 유파만 슬롯에 장착 가능(저위/공용은 자유 혼합). — [[06_skill_style_system]]
- 스킬 구성 요소 확정 (다크소울 영감): 스킬은 데미지/사정거리/효과로 구성, 스킬마다 영향받는 스탯과 스탯별 스케일링 등급이 다름 (예: 힘 A급/기량 C급). 실제 스탯 목록은 미정. — [[06_skill_style_system]]
- 세계관 톤 확정: 던전 밖(밝고 인간적) vs 던전 안(차갑고 인간 불신, 죽음이 무의미)의 강한 대비. 다크소울류의 전면적 암울함은 지향점 아님. 사람들이 던전에 들어가는 이유는 미정. — [[00_game_overview]], [[07_progression_death_system]]
- 진행 구조 확정: 런 기반 로그라이크 + 익스트랙션. 특정 층/장소에서 마을로 귀환 가능, 던전에서 사망 시 소지 아이템 전부 상실, 다음 런에서 사망 지점 아이템 회수 가능. — [[07_progression_death_system]]
- 플레이어 캐릭터 특수성 확정: 던전에서 죽어도 마을로 돌아올 수 있는 것은 플레이어뿐, 파티원/다른 NPC는 진짜로 죽음. 이유는 세계관 설정 과제로 남김. — [[07_progression_death_system]]
- 파티원 사망 처리: 퍼머데스/부상 복귀 여부는 "상황에 따라 다름"으로 방향만 결정, 조건은 미정. — [[07_progression_death_system]]
- 주인공 정체성 확정: 커스터마이징 아바타, 전투 직접 참전 (지휘관형 아님). — [[00_game_overview]]
- 던전 기원 확정: 초월적 존재가 만든 공간 + 고대 유적 + 외부 세계 요소의 혼합. — [[08_world_and_factions]]
- 던전 입장 동기 확정(기본형): 희귀 자원/재화. 보물·몬스터 부산물이 외부 세계에서 다양하게 활용됨. 깊이 내려갈수록 보상↑, 몬스터 강함↑ (위험-보상 스케일링, 런/익스트랙션 구조와 연동). — [[08_world_and_factions]], [[07_progression_death_system]]
- 세력(단체) 성격 방향 확정: 집단마다 뚜렷이 다른 성격. 예시로 탐험가 집단/외부로 못 나가는 범죄자 집단/던전의 진실을 파헤치는 집단/종교적 목표로 종군하는 이들/귀족가 지원 탐사대/강함 자체를 연구하는 이들 등이 나옴 (최종 목록 아님). — [[08_world_and_factions]]
- **보류 중인 결정**:
  - 플랫폼 (웹 vs 데스크톱 앱) — [[04_tech_stack]]
  - 승격 트리거 조건, 기억 데이터 구조 세부안 — [[01_relationship_system]]
  - 개인 특성치 목록 확정, 특성-관계 상호작용 계산식, 네임드 특성 변화 트리거, 관계도 표시 방식(수치 vs 등급) — [[01_relationship_system]]
  - 전투 세부 규칙 (그리드 크기, 사거리, 지형효과 등) — [[03_combat_system]]
  - 유파 구체적 이름/컨셉 목록(규모 6~9개는 확정), 입문 조건, 숙련도 획득 방식, 스킬 슬롯 개수, 고위 기술 등급 기준 — [[06_skill_style_system]]
  - 스탯 7~8개로 확장할지 여부(핵심 6개는 확정), 스케일링 등급 체계 — [[03_combat_system]], [[06_skill_style_system]]
  - 관계 수치의 개인-단체 이중구조 정확한 가중치 계산식 — [[01_relationship_system]]
  - 파티원 사망의 퍼머데스/복귀 경계 조건, 익스트랙션 지점 형태, 사망 지점 아이템 회수 시스템 세부 — [[07_progression_death_system]]
  - 던전 기원 3요소가 구체적으로 어떻게 표현되는지(구역별 테마 구분 여부), 세력 목록 최종 확정과 세부 설정, 세력 간 관계 축 — [[08_world_and_factions]]
  - 프로젝트 가제
  - 캐릭터 스프라이트 해상도 및 그리드 칸 크기(둘을 연동해서 결정 필요 — 직접 여러 시안을 그려보고 테스트한 뒤 확정하기로 방향만 정함, 32x32는 2026-08-10 테스트용 임시값일 뿐 확정 아님) — [[03_combat_system]], [[09_aseprite_pixel_pipeline]]
  - 플레이어 캐릭터 장비 커스터마이징의 시각적 반영 방식(제안됨, 미확정): 인게임 그리드 스프라이트는 장비 변화를 색상/실루엣 정도로 단순화하고, 장비 디테일을 제대로 보여주는 건 이미 결정된 페인팅 초상화 쪽에 집중 — 애니메이션 프레임마다 장비 조합을 전부 다시 그려야 하는 조합 폭증을 피하기 위함. 사용자가 검토 중, 아직 확정 아님. — [[00_game_overview]], [[09_aseprite_pixel_pipeline]]
  - 전투/메뉴용 입력 액션 및 키 배치 — 이동/상호작용(WASD/E)만 2026-08-11에 확정, 전투 조작·인벤토리·메뉴 등은 해당 시스템이 아직 없어 의도적으로 보류. — [[03_combat_system]]

## 2026-08-09 (개발 환경 및 전투 시스템 상세화)
- 개발 환경: Godot 4.7.1 설치 완료(winget). git init, 실제 구현은 아직 미착수.
- 던전 내 이동 방식 확정: 탐사는 자유 이동(8방향), 적과 시야 접촉 시(필드 조우식) 심리스하게 그리드 전투로 전환(별도 로딩 없이 현재 필드가 그대로 전투 그리드가 됨). — [[02_dungeon_town_structure]], [[03_combat_system]]
- 파티 규모 확정: 초반 소규모(2~3명) → 중반 중규모(4~6명)로 성장, 솔로 플레이도 지원(자체 하드모드). 몬스터 수는 파티보다 많을 수 있음. — [[03_combat_system]]
- 그리드 크기 방향: 유닛 수/환경에 따라 가변, 최소 중형 이상. 정확한 치수는 미정. — [[03_combat_system]]
- 턴 순서 확정: 개별 유닛 속도 스탯 기반 우선순위(초리어티) 큐. — [[03_combat_system]]
- 사거리/시야 확정: 엄폐 구현 + 단순화된 고저차(1/2/3단), 고지대 원거리 공격에 사거리·명중 보너스, 등반 시 이동력 2배 소모. 부담이 크면 고저차는 빼고 엄폐만 남기는 것도 대안. — [[03_combat_system]]
- 지형 효과 확정: 이동 방해 지형 + 상태이상 지형(장판) + 아이템으로 칸 상태를 일시 변경하는 시스템, 그리드 칸 단위 상태 저장 전제. — [[03_combat_system]]
- 스탯 체계 방향: 다크소울류 중간 규모(6~8개) 스탯. 정확한 목록은 [[06_skill_style_system]] 스킬 스케일링과 함께 추후 확정. — [[03_combat_system]]
- 관계 수치 구조 확정: 개인 + 소속 단체 이중구조. 단체 평판이 소속 NPC 전원의 초기 호감도에 가중치를 줌. 정확한 계산식은 미정. — [[01_relationship_system]]
- 스킬 계열 확정: 순수 물리/기술 + 기존 마법류 + 던전 기원 신마법류의 3계열 체계. 던전 기원 신마법은 세계관의 초월적 던전 기원과 직결. — [[06_skill_style_system]], [[08_world_and_factions]]

## 2026-08-10
- 스탯 목록 핵심 6개 확정: **체력 / 힘 / 기량 / 지능 / 신앙 / 신비**. 물리 계열은 체력/힘/기량, 기존 마법류는 지능/신앙(유파마다 비중 다름), 던전 기원 신마법류는 신비 비중이 상대적으로 높은 경향(전용은 아님). 7~8개로 확장할지는 미정. — [[03_combat_system]], [[06_skill_style_system]]
- 유파 규모 확정: 초기 출시 기준 소규모(총 6~9개, 계열당 2~3개). 1인 개발 콘텐츠 부담을 고려, 출시 후 확장 가능한 구조를 전제로 함. 구체적 유파 이름/컨셉은 미정. — [[06_skill_style_system]]
- 비주얼 스타일 방향 수정: "2D로 시작 후 여력되면 2.5D 검토"에서 **처음부터 2.5D(HD-2D) 지향**으로 변경. 캐릭터는 2D 도트 스프라이트 유지 + 동적 그림자, 일부 배경/외부 요소는 3D처럼 보이도록 연출. 엔진이 결국 2D 기반이라 전부 시각적 트릭(SubViewport + 3D 라이팅 조합 등)에 의존하는 것을 전제로 함. 어느 요소를 3D로 처리할지 등 세부 구현 방식은 미정. — [[00_game_overview]], [[04_tech_stack]]
- 아트 파이프라인 확정(뒤늦게 문서화, 결정 자체는 이전 세션에서 확정됨): 베이스 스프라이트는 픽셀아트, 네임드 NPC는 대화용 페인팅 버스트 초상화를 별도 트랙으로 제작(AI 보조). 도구는 Aseprite + `pixel-plugin`(Aseprite MCP) 조합. 명령어/팔레트 설명은 신규 문서로 분리. — [[09_aseprite_pixel_pipeline]]

## 2026-08-11
- 플레이어 사망 시 귀환 가능한 이유 확정: **"세계 충돌" 이론.** 던전과 결부된 초월적 존재가 주인공을 낙점, 주인공이 던전에서 사망하면 그 세계선에서는 진짜로 죽되, 초월적 존재가 주인공이 죽지 않은 다른 세계선을 억지로 충돌·융합시켜 살림 — 이 과정에서 살아있던 세계선 쪽 의식은 지워짐. 그 런에서 쓴 장비/소비 아이템/재화가 복구되지 않는 것은 이 충돌의 부작용으로 자연스럽게 설명됨(마을 자원이 "안 쓴 셈"으로 되돌아가지 않음). 파티원/다른 NPC는 낙점 대상이 아니므로 진짜로 죽는 기존 비대칭과 정합. 왜 하필 주인공을 택했는지, 지워지는 의식의 윤리적 함의, 이 현상이 다른 존재에게 알려지는지는 의도적으로 미스터리로 남김. — [[07_progression_death_system]], [[08_world_and_factions]]
- Godot 구현 착수: 씬 전환(마을↔던전, `GameManager`가 컨테이너 스왑), 3D 오블리크 카메라(직교 투영, 25도 — 첫 시도값인 35도에서 낮춤) + 빌보드 스프라이트 캐릭터로 세계 렌더링 구조 확정(평평한 2D+가짜 그림자 대신 실제 3D 지형 채택 — 이미 확정된 고저차/엄폐 전투 규칙을 자연스럽게 구현하기 위함). 카메라는 Player의 자식으로 둬서 씬 불문 자동 추적. 마을/던전 각각 위치 기반 상호작용 지점(진입/추출 지점)에서만 씬 전환 가능하도록 구현 — 기존 "특정 장소에서만 귀환 가능" 규칙과 일치. 정식 키 바인딩은 보류, Godot 기본 `ui_*` 액션으로 임시 구현(TODO로 코드에 표시). — [[03_combat_system]], [[07_progression_death_system]]
- 관계 수치 세부 구조 확정: 단일 스칼라(v1, 다차원으로 확장 가능하게 구조화), 개인 호감도는 "씨앗(첫 만남 시 단체 평판 반영) + 지속적 견인(이후 단체 평판 변화에 다소 끌려가되, 개인 호감도가 중립에서 멀수록 저항 커짐)" 하이브리드로 결정. 극값(±100)에서 단체 평판 영향에 사실상 면역되는 것은 의도된 동작(단, 극값 도달이 실제로 어렵게 밸런싱됐을 때만 타당 — 밸런싱 재검토 여지 있음). 정확한 씨앗 가중치/견인 곡선 수치는 밸런싱 영역이라 미정, 구현 시 별도 함수로 분리. 개인 특성(탐욕/이타심 등)은 관계 데이터가 아니라 NPC 자체 데이터에 별도로 둠. — [[01_relationship_system]]
- NPC/단체 정적 데이터 포맷 확정: Godot `Resource` 커스텀 클래스(`NPCData`, `FactionData`)로 구현, `data/npcs/`·`data/factions/`에 `.tres` 파일로 저장. 익명 랜덤 NPC는 승격 전까지 `NPCData` 없이 `RelationshipManager`만으로 취급(가벼움 유지), 승격되거나 사전 제작된 네임드 NPC만 `NPCData` 인스턴스를 가짐. 특성 필드는 목록 미확정이라 `Dictionary`로 열어둠.
- 정식 입력 액션 확정: **이동은 WASD, 상호작용은 E**(전용 `interact` 액션 신설, 기존 `ui_accept` 재사용 중단). 전투/메뉴용 키는 해당 시스템이 아직 없어 미리 정하지 않고 보류 — 필요해질 때 그 시스템과 함께 설계. `project.godot`에 `[input]` 섹션 추가, `Player.gd`/`Town.gd`/`Dungeon.gd`를 새 액션으로 교체.
- 던전 클리어 엔딩 구조(아이디어 단계) 확장: 히든 엔딩 2를 **2-a(타락)/2-b(진짜 해방)로 분리**. 2-a는 접근이 쉬운 **광신도 진영**과 손잡고 가디언을 쓰러뜨려 세상이 던전에 소멸당하는 엔딩, 2-b는 접근이 어려운 **던전의 진실을 파헤치려는 집단**(생존자+또 다른 선택받은 강자)과 함께 "가디언과 던전(블랙홀)이 원래 하나였다가 갈라진 존재"라는 진실을 알아내 둘을 합쳐 쓰러뜨림으로써 신적 존재에 좌우되지 않는 진짜 해방을 맞는 엔딩. 이 분리로 저번에 미정이었던 "단체 정체 후보 4개 중 택1" 문제가 자연스럽게 해소됨(후보들을 서로 다른 두 세력에 나눠 재활용). 추가로, 기존 "종교적 목표로 종군하는 이들" 세력 예시를 **광신도 진영**(엔딩 2-a 연결)과 **선의의 신앙 집단**(가디언과는 다른, 실존 여부 불명확한 신을 믿고 선행하는 일반 신앙 공동체, 엔딩과 무관한 텍스처용)으로 분리 — 신앙을 가진 이들이 전부 위험한 광신도는 아니라는 대비를 주기 위함. — [[08_world_and_factions]]
- Godot 구현(그리드 이동): 크기 무관한 월드↔그리드 셀 좌표 변환 유틸(`Grid.gd`, `CELL_SIZE`는 튜닝 가능한 플레이스홀더) + 바닥에 그리드 라인 시각화(전투 모드일 때만 표시, 셀 "경계"에 선을 그려서 정수 좌표가 셀 "중심"이 되도록 함 — 처음엔 선을 정수 좌표에 그려서 캐릭터가 꼭짓점에 서는 버그가 있었고 수정함). `Player`가 자유 이동/그리드 이동 두 모드를 가지며, 던전에 배치한 적(플레이스홀더, 스켈레톤 스프라이트 재사용) 근접 시 자동으로 그리드 전투 모드 진입 — 실제 인카운터 트리거가 생기기 전까지의 임시 대역. Q키로 수동 토글도 유지(테스트 편의용). — [[02_dungeon_town_structure]], [[03_combat_system]]

## 2026-08-13
- NPC 조우 방식 확정: 던전과 마을이 구조적으로 다름. **던전**은 기존에 확정된 랜덤/테이블-트리거 NPC 분류 그대로(맵 위를 돌아다니는 개체, 비적대적이면 대화 가능). **마을**은 NPC를 맵에 직접 배치하지 않고, 특정 시설(술집/상점/창고 등) 입장 시 그 시설에 "있는" NPC와 대화하는 로스터 구조. 파티원은 100% 인카운터(전용 장소 방향, 세부 미정). 비파티원 NPC의 시설 출현 여부는 **퀘스트 상태 > 조건 충족 > 랜덤** 우선순위로 결정(확정) — 조건의 구체적 종류, 랜덤 확률값, 파티원 전용 장소의 형태는 미정. — [[02_dungeon_town_structure]]
- 스탯 체계에 **속도**를 7번째 핵심 스탯으로 추가 확정. 계기: 초리어티 큐(턴 순서)가 이미 "속도 스탯 기반"으로 결정돼 있었는데 확정된 6개 스탯엔 속도가 없던 정합성 문제를 잡음. 전투용 실질 속도는 `기본 속도 스탯 + 기량 구간별 보너스(계단식) + 장비 보정`의 합성값으로 결정 — 기량 구간 경계/증가폭은 밸런싱 영역이라 미정, 구현 시 별도 테이블로 분리. — [[03_combat_system]]
- Godot 구현: 스탯 시스템(`Stats`+`CombatFormulas`), 초리어티 큐(`TurnQueue`, CT 누적식, 초과분 이월), 인벤토리 기초(`ItemData`+`Inventory`) 구현. 인벤토리는 **시스템(순수 데이터/로직, UI 의존성 전혀 없음)과 UI를 시그널로 분리** — `Inventory`는 `contents_changed` 시그널만 쏘고, 나중에 리소스가 생겨 UI를 붙이거나 교체할 때 시스템 코드를 안 건드려도 되게 함. `Inventory.clear()`가 곧 "던전 사망 시 소지 아이템 전부 상실" 규칙의 구현체. 인벤토리 용량 등 수치는 플레이스홀더. — [[03_combat_system]], [[07_progression_death_system]]
- Godot 구현: 최소 전투 루프(공격→데미지→사망→루팅) 연결. 정식 스킬 시스템([[06_skill_style_system]] 데미지/사거리/효과) 대신 **틀만 먼저 잡는 방향으로 진행** — 데미지 계산을 `CombatFormulas.basic_attack_damage()` 하나로 분리해서, 나중에 실제 스킬 데이터가 생기면 이 함수만 교체하고 전투 루프(턴 진행/데미지 적용/사망 판정)는 그대로 두는 걸 전제로 함. **몬스터 라이프사이클** 확정: `MonsterData`(정적 정의: 스탯, 드랍 테이블)+`Monster`(런타임, `ALIVE`→`DEAD` 상태) 구조. 사망 시 드랍 테이블을 굴려 루팅 가능한 시체가 되고(파괴되지 않고 필드에 남음), 플레이어가 상호작용(E)하면 루팅되어 `Inventory`로 들어가고 시체는 소진됨. — [[03_combat_system]], [[06_skill_style_system]]
- Godot 구현: **몬스터 반격 연결.** 플레이어도 HP(`current_hp`, 체력 스탯 기반)를 갖게 되고, 몬스터 턴이 오면 몬스터의 유일한 행동인 "플레이어 공격"이 실행됨(같은 `basic_attack_damage()` 재사용, 공격자만 다름). 플레이어 HP가 0이 되면 **"생존 후 마을로 후퇴 + HP 전체 회복"** 플레이스홀더로 처리 — 진짜 사망 결과(아이템 상실 등)는 "세계 충돌" 시스템([[07_progression_death_system]]) 몫이라 아직 연결 안 함. 이 과정에서 `TurnQueue` 연동 버그를 하나 발견해 고침: 플레이어가 공격한 직후 `_current_turn_id`가 아직 "player"로 남아있는 상태라 몬스터 턴으로 못 넘어가던 문제(자기 턴을 "소비"하는 `advance()` 호출이 빠져있었음) — 이 때문에 지난 항목에서 "몬스터 AI 없어서 즉시 통과"라고 적었던 게 사실 몬스터 턴 자체가 아예 실행이 안 되고 있었던 것으로 드러남.
- Godot 구현: **그리드 이동을 WASD 한 칸씩 이동에서 마우스 클릭 이동으로 교체.** 카메라에서 마우스 위치로 레이캐스트 → 바닥 평면 교차점 → 그리드 셀 변환(`Grid.raycast_to_cell()`, `Player`/`GridOverlay` 공용). 클릭한 칸으로 즉시 이동(경로 계산 없음). `GridOverlay`에 마우스가 가리키는 칸을 보여주는 반투명 하이라이트 추가. 이동 거리 제한("이동력") 시스템은 아직 없어서 클릭한 아무 칸이나 즉시 이동 가능 — 추후 이동력 시스템과 함께 제한 예정. 자유 이동(탐사)은 그대로 WASD 유지, 그리드(전투) 모드만 교체됨. — [[03_combat_system]]
- Godot 구현: **인벤토리 표시용 최소 UI(`InventoryPanel`) 연결.** 아이콘/그리드 레이아웃 없이 순수 텍스트 목록만 보여주는 플레이스홀더, `I` 키로 토글. `Inventory.contents_changed` 시그널을 구독해서 갱신 — Player와 직접 결합하지 않고 `bind(inventory)`로 주입받는 구조라, 나중에 진짜 리소스가 생기면 이 씬 파일 하나만 통째로 교체하면 됨(Inventory 시스템 코드는 안 건드림). Player의 자식으로 둬서 마을/던전 어디서든(플레이어가 씬을 옮겨다녀도) 계속 쓸 수 있음. — [[03_combat_system]]
- **인벤토리 시스템 설계 확정**(신규 문서 [[10_inventory_system]]): 형태는 그리드/나열식 대신 **자유 배치형**(울티마 온라인류, 겹침 방지 없음). 용량은 슬롯 개수가 아니라 **무게 총합**으로 제한하고, 초과 시 실질 속도에 페널티(`CombatFormulas.effective_speed()`의 `equipment_modifier` 자리와 연결 예정). **전투 중 투척** 확정: 인벤토리 아이템을 그리드 타일로 드래그하면 그 위치에 던짐(`Grid.raycast_to_cell()` 재사용). **인접 배치 위험**(불+폭탄=폭발, 날카로움+물약=파손 등) 방향 확정 — 배치 자체를 막지 않고 실제로 결과가 발생하게 하되(세계관의 하드코어 톤과 일치시키기 위함, 사용자가 던전 탐험을 더 하드코어하게 만들고 싶어함), 완전히 예측 불가능하지 않도록 아이템 태그/경고 UI로 "주의하면 피할 수 있었다"는 학습 곡선을 의도함. 세부(태그 목록, 규칙 테이블, 경고 UI 형태, 무게/투척 수치)는 전부 미정. — [[03_combat_system]], [[07_progression_death_system]]
- Godot 구현: **인벤토리 UI를 텍스트 나열에서 실제 자유 배치 UI로 교체.** `InventorySlotIcon`(드래그 가능한 개별 아이템 아이콘) + `InventoryPanel`의 `Bag` 영역(자유 배치 컨테이너)으로 구성. 아이템 아이콘 리소스가 없어서 `item.icon`이 비어있으면 **일반 색상 사각형으로 대체**하는 폴백을 넣음(뼛조각 등 지금 아이템 전부 해당) — 나중에 진짜 아이콘이 생기면 `ItemData.icon`만 채우면 자동으로 전환됨, 코드 변경 불필요. 드래그는 Godot 기본 Control 드래그 API 대신 **직접 구현**(마우스 down/motion/up 수동 처리) — 나중에 전투 그리드 타일 위로 드롭하는 것도 처리해야 하는데, 이건 다른 Control이 아니라 3D 월드 좌표로 드롭하는 거라 Godot 기본 DnD로는 못 다룸. `Inventory.slots`에 **`slot_id`**(스택별 안정적 식별자, 배열 인덱스는 슬롯이 추가/제거되며 밀려서 UI가 위치를 추적하기에 부적합) 추가 — UI가 슬롯 위치를 `slot_id` 기준으로 저장. 겹침 방지 없음(설계대로), 패널 밖으로 드래그하면 패널 안으로 클램프. 전투 중 투척은 아직 미연결(`_on_icon_drag_ended`에 TODO로 표시) — 다음 작업. — [[10_inventory_system]]
- Godot 구현: **전투 중 투척 연결.** `ItemData`에 `throwable`(옵트인 불리언) + `throw_damage`(고정 데미지, `basic_attack_damage()`와 같은 성격의 플레이스홀더) 추가, 뼛조각을 테스트용으로 던질 수 있게 설정. `InventoryPanel`이 인벤토리 패널 바깥으로 드롭된 걸 감지하면(`_bag`의 화면 영역 밖 + 전투 중 + 아이템이 throwable) `Grid.raycast_to_cell()`로 드롭 위치를 셀로 변환해 `item_throw_requested` 시그널만 쏘고, 실제 효과(그 칸에 적이 있으면 데미지 적용)는 `Dungeon.gd`가 처리 — UI는 "의도"만 알리고 게임 로직은 씬이 담당하는 분리 유지. **빗나가도 아이템/턴은 소모됨**(하드코어 톤 유지). `_do_attack()`과 던지기가 턴 진행 로직(적 턴 통과 처리)을 공유하도록 `_advance_past_enemy_turn()`으로 리팩터링해서 중복 제거. 헤드리스 테스트로 명중/빗맞음/전투 밖 무시/반복 투척으로 실제 처치까지 전부 검증. — [[10_inventory_system]]

## 2026-08-18
- **버그 진단**: "스켈레톤 잡고 마을 갔다가 다시 던전 가서 아이템 던졌더니 갑자기 마을로 이동됨" 버그 리포트. 원인은 승리 후 HP를 회복시키는 코드가 어디에도 없다는 것 — `Player.heal_to_full()`은 `Dungeon._handle_player_defeat()`(패배 시 "생존 후퇴" 처리)에서만 호출됨. `Player` 노드는 씬 전환 때 파괴되지 않고 재부모화만 되므로 `current_hp`는 마을을 거쳐도 그대로 유지되는데, 전투 중이 아니면 HP를 볼 방법이 전혀 없어서(라벨이 전투 모드 UI에만 있음) 첫 전투에서 입은 데미지가 조용히 누적되다가 두 번째 전투에서 반격 한 번(이번엔 투척으로 트리거된 몬스터 턴)에 HP가 0이 되어 예고 없이 "패배" 처리된 것. HP가 승리해도 자동 회복되지 않는 것 자체는 하드코어 톤과 부합하는 의도된 설계로 볼 수 있음 — 진짜 문제는 **가시성 부재**였음.
- **수정**: 항상 보이는 HP 표시(`PlayerHUD`, `scenes/ui/PlayerHUD.tscn`+`.gd`) 추가. `InventoryPanel`과 동일한 패턴(Player의 자식으로 씬에 상주, `bind(player)`로 주입받아 매 프레임 라벨 텍스트 갱신)으로 구현해서 마을/던전/전투 어디서든 항상 노출됨. 헤드리스 스모크 테스트로 (1) 초기 HP 표시, (2) 데미지 직후 갱신, (3) 씬 전환(마을→던전) 후에도 HP 유지 및 HUD 반영을 검증. — [[03_combat_system]]
- **테스트 방법론 갱신**: 이번 스모크 테스트에서 `--quit` 플래그를 쓰면 첫 프레임 직후 강제 종료되어 `await get_tree().process_frame`을 쓰는 비동기 테스트 스크립트가 끝까지 실행되지 못하는 것을 확인(출력이 전혀 안 찍힘). `--quit` 없이 실행하고 테스트 스크립트 자신이 `get_tree().quit()`을 호출해 끝내는 방식으로 전환해야 함 — 임시 메인 씬 스왑 기법 자체는 유효하지만, 여러 프레임에 걸친 비동기 테스트에는 `--quit`을 함께 쓰면 안 됨.
- Godot 구현: **마을 시설 로스터(v1) 연결.** 두 핵심 축 중 아직 게임 안에서 실제로 동작하지 않던 관계 시스템 쪽을 처음으로 연결. `FacilityRoster.resolve_present_npcs()`(순수 로직, `RefCounted`)가 퀘스트>조건>랜덤 우선순위 구조를 코드로 갖추되, 퀘스트/조건 시스템이 아직 없어 두 훅 모두 항상 `false`인 스텁으로 남기고 **랜덤 티어만 실제 동작**(`NPCData.appearance_chance`) — 사용자와 논의 후 확정한 v1 스코프. 대화도 실제 시스템 없이 **이름+고정 인사말 스텁**(`DialogueBox`)으로만 구현, 선택지/퀘스트/거래 연동은 별도 작업으로 명시적으로 미룸. `TownFacility`(씬에 배치되는 마커, `town_facility` 그룹) 구조로 시설을 여러 개 확장 가능하게 하고, 테스트용으로 선술집 + NPC 2명(`data/npcs/tavern_keeper.tres`, `tavern_patron_dorn.tres`)을 배치. 첫 대면 시 `RelationshipManager.register_npc()` 자동 호출 확인. 헤드리스 스모크 테스트로 확률 극값(0.0/1.0) 결정론, 빈 로스터/있는 로스터 각각의 대화 표시, 관계 등록, 던전 입구 회귀까지 전부 검증. — [[01_relationship_system]], [[02_dungeon_town_structure]]
- Godot 구현: **마을 시설 로스터를 목록형 UI로 확장.** 이전 버전은 시설에 여러 NPC가 동시에 있어도 항상 후보 배열 0번(그레타)만 보여주는 숨은 제약이 있었는데(사용자가 실제로 겪고 지적함), 원래 구상하던 "시설 내부에 NPC가 자리에 배치되어 있고 클릭해서 대화" 방식은 캐릭터 스탠딩/배경 아트가 아직 없어 지금 만들면 결국 또 플레이스홀더만 배치하는 셈이라 실익이 작다고 판단 — 대신 아트가 필요 없는 **목록형 UI**로 대체하기로 논의 후 결정. `DialogueBox`가 present NPC 배열을 받아 1명이면 목록 없이 바로 인사말로, 2명 이상이면 클릭 가능한 이름 버튼 목록(+뒤로가기)으로 진입하도록 재작성. `Town.gd`와의 연결은 `InventoryPanel`과 같은 시그널 기반 분리 패턴을 따름 — `DialogueBox`는 `npc_selected` 시그널만 쏘고, `RelationshipManager.register_npc()` 호출 등 실제 부수효과는 `Town.gd`가 담당. 이렇게 로직과 UI 표현을 분리해뒀기 때문에, 나중에 캐릭터 아트가 생겨 인테리어 클릭 방식으로 바꾸더라도 `FacilityRoster`/`Town.gd`는 그대로 두고 `DialogueBox` UI만 교체하면 됨. 헤드리스 스모크 테스트로 다중 노출(목록+버튼 클릭+등록+뒤로가기), 단일 노출(목록 생략), 빈 로스터, 던전 입구 회귀까지 전부 검증. — [[01_relationship_system]], [[02_dungeon_town_structure]]
- **전투 시스템 대전환: 그리드 폐기 → 자유 이동 + 스테미나(신규 8번째 핵심 스탯).** 계기: 사용자가 "기술 범위를 구(반경)로 판정하면 그리드 없이도 되지 않나?"라고 제안. 검토 결과 지금이 전환 적기라는 데 합의 — 스킬 시스템도 던전 절차적 생성도 아직 코드가 하나도 없어서 그리드를 전제로 먼저 만들면 나중에 두 번 일하게 됨. 클릭 이동에 경로탐색이 필요한 건 그리드였어도 마찬가지였을 것이라는 점, 엄폐/시야도 그리드 셀 플래그보다 실제 3D 지오메트리 레이캐스트가 더 자연스럽다는 점도 결정에 힘을 보탬. 그리드는 애초에 "별 생각 없이 정한 초기값"이었을 뿐 지켜야 할 취향적 이유가 없다고 확인함(건담 G 이터널 비주얼 레퍼런스와 느낌이 달라지는 트레이드오프는 인지하되 문제 없다고 판단).
  - **이동**: 전투 중에도 클릭한 지점으로 자유 이동(그리드 스냅 없음). 경로탐색은 `NavigationAgent3D`로 구현 예정(설계만, 미구현).
  - **스테미나(신규 핵심 스탯, 8번째)**: 이동 거리 비례 소모(고지대일수록 추가 소모, 기존 "등반 시 이동력 2배" 대체) + 공격/스킬 사용 시 소모. 매 턴 시작 시 전량 리필(이월 없음). 속도(턴 순서 전용)와는 역할을 분리한 별도 스탯으로 확정 — 속도에 얹으면 빠른 캐릭터 가치가 턴 순서+행동력 이중으로 뻥튀기되는 문제가 있어서 분리를 선택. 목적은 원거리 캐릭터의 "이동-공격" 무한 반복(카이팅)을 자연스럽게 억제하고 매턴 이동/행동 배분을 전략적으로 고민하게 만드는 것.
  - **턴 구조**: 기존 "행동 1회 = 턴 종료"에서 **플레이어가 직접 "턴 종료"를 눌러야 끝나는 구조**로 변경 — 스테미나가 남아있으면 이동+행동을 여러 번 섞어 쓸 수 있어야 하므로.
  - **스킬 범위**: 사거리(시전자→목표 지점 최대 거리) + 효과반경(그 지점 중심 영향 범위)의 이원 구조로 통일, 효과반경 0=단일 타겟/0 초과=광역기. 판정은 XZ 평면 거리 기준(고저차 Y 무시, `Grid.gd`가 이미 하던 평면 가정 계승). 지금 있는 뼛조각 투척도 이 구조에 들어맞음(사거리=투척거리, 효과반경=0) — 단, 실제 재구현은 아직 미착수.
  - 이번 세션은 설계/문서화만 진행, **코드는 아직 안 건드림** — 구현은 다음 작업. — [[03_combat_system]], [[06_skill_style_system]], [[10_inventory_system]]

## 2026-09-16
- Godot 구현: **08-18에 확정한 그리드 폐기 → 자유 이동+스테미나 전환을 실제로 적용.**
  - `Stats.gd`에 `stamina`(기본 100 — 다른 스탯의 10점 스케일과 달리, 이동/공격 소모 단위 비용이 자연스러운 값이 되도록 스케일을 다르게 잡음) 추가.
  - `CombatFormulas.gd`에 `MOVE_STAMINA_COST_PER_UNIT`/`BASIC_ATTACK_STAMINA_COST` 플레이스홀더 상수와 `movement_stamina_cost()`/`max_move_distance()` 추가.
  - `scripts/systems/Grid.gd` → **`Targeting.gd`로 교체**(셀 변환 함수 전부 제거, `raycast_to_floor_point()`로 월드 좌표 직접 반환 + `flat_distance()`로 XZ 평면 거리 계산 — 스킬 범위 구조의 판정 기준). `Player.gd`/`InventoryPanel.gd`/`Dungeon.gd`가 전부 이걸로 갈아탐.
  - `Player.gd`: `MovementMode.GRID` → `COMBAT`으로 개명. 전투 중 클릭 이동은 이제 셀 스냅이 아니라 클릭한 지점으로 스테미나가 허용하는 만큼 이동(멀리 클릭해도 갈 수 있는 만큼만 이동 — "클램프", 거부 아님). `is_my_turn`(전투 스크립트가 세팅) 플래그를 추가해 자기 턴이 아니면 클릭 이동 자체가 안 되게 막음 — 이전엔 이동이 턴 순서를 전혀 신경 안 썼던 구멍이 있었는데 이번에 같이 막힘. `start_turn()`/`end_turn()`으로 스테미나 리필/턴 활성 상태를 관리.
  - `scenes/dungeon/GridOverlay.gd` → **`MovementRangeIndicator.gd`로 교체**: 그리드 선 대신 플레이어 위치 중심의 원반(반지름 = 남은 스테미나로 갈 수 있는 거리)을 보여줌, 정확한 클램프 지점 프리뷰(호버 마커)는 이번엔 스코프에서 뺌(나중에 필요하면 추가).
  - `Dungeon.gd`: **턴 구조를 "행동 1회=턴 종료"에서 명시적 종료로 변경** — 새 `end_turn` 입력 액션(F 키) 추가, `_do_attack()`/`_on_item_thrown()`이 더 이상 턴을 자동으로 넘기지 않음. 공격은 스테미나 부족하면 거부(사거리 체크는 원래 있던 `INTERACT_RANGE` 근접 판정을 그대로 "단일 타겟 사거리"로 재해석해서 재사용). `_enter_combat()`과 `_end_turn()`이 중복 로직 없이 같은 `_resolve_enemy_turns()`를 공유하도록 리팩터링 — 겸사겸사 "적이 먼저 턴을 잡으면 아무 반응 없이 멈추는" 잠재 버그도 같이 고침(이전엔 콤벳 진입 시 첫 턴이 적이면 그 턴을 실행하는 코드가 없었음).
  - `Dungeon.gd`의 투척(`_on_item_thrown`)을 그리드 셀 일치 판정에서 사거리(`item.throw_range`, 범위 밖이면 아이템/스테미나 소모 없이 그냥 거부)+효과반경(`item.effect_radius`, 범위 안인데 빗나가면 그래도 아이템/스테미나 소모) 판정으로 재구현. `ItemData.gd`에 두 필드 추가(둘 다 밸런싱 placeholder).
  - `InventoryPanel.gd`의 투척 드롭도 셀 대신 월드 좌표(`Vector3`)를 실어 보내도록 시그널 시그니처 변경.
  - 헤드리스 스모크 테스트로 이동 클램프(범위 초과 클릭 시 딱 갈 수 있는 만큼만 이동, 스테미나 0에선 전혀 이동 안 함), 공격의 스테미나 게이팅(부족하면 거부/충분하면 소모+데미지+턴 안 넘어감), 투척의 사거리 거부/효과반경 판정, 턴 종료→적 턴→플레이어 턴 복귀+스테미나 전량 리필, 자기 턴 아닐 때 이동 완전 차단까지 전부 검증. — [[03_combat_system]]
- Godot 구현: **이동 되돌리기(undo) 추가.** 사용자 아이디어: 공격 전까지는 이동(스테미나 포함)을 자유롭게 되돌릴 수 있어야 실수로 잘못 이동해도 부담이 없는데, 공격까지 무를 수 있게 하면 "찔러보고 결과 나쁘면 취소"하는 전략적 이점이 너무 커지므로 공격(또는 투척) 실행 순간부터는 그 턴이 끝날 때까지 되돌리기를 완전히 잠금. `Player`에 `_turn_start_position`/`_has_acted_this_turn` 추가 — `start_turn()`이 둘 다 리셋(턴 시작 위치 기록 + 잠금 해제), `mark_acted()`(공격/투척이 실제로 실행됐을 때만 `Dungeon.gd`가 호출 — 사거리 밖이라 거부된 시도는 호출 안 됨)가 잠금, `undo_movement()`는 잠기지 않았을 때만 위치+스테미나를 턴 시작 상태로 **한 번에 전부 리셋**(단계적 undo 아님, 사용자가 명시적으로 선택). `undo_movement` 입력 액션(R 키) 추가.
- **테스트 방법론 갱신(중요)**: 이번 헤드리스 테스트 중 프로세스가 몇 분간 멈춘 것처럼 보인 사고가 있었음 — 원인은 `assert()` 실패였음. GDScript의 `assert()`가 실패하면 그 호출 스택(`_ready()` 코루틴)은 거기서 조용히 중단되지만, **엔진 자체는 계속 실행됨** — 헤드리스라 창도 없고 할 일도 없어서 `get_tree().quit()`이 호출될 차례가 영영 안 오는 채로 프로세스가 무한히 idling함. 콘솔에 `SCRIPT ERROR: Assertion failed: ...`가 찍히긴 하지만, 파이프로 리다이렉트하면 버퍼링 때문에 프로세스가 끝나기 전엔 안 보임 — "출력이 하나도 안 찍히고 몇 분째 안 끝남"이 곧 "테스트 코드에서 assert 실패, 프로세스는 안 죽고 그냥 대기 중"의 신호. 앞으로 헤드리스 테스트가 타임아웃되면 진짜 무한루프로 단정하지 말고, 프로세스를 강제 종료한 뒤 그때까지의 출력을 먼저 확인할 것. (이번 사고의 실제 원인은 테스트 스크립트 자체의 버그—공격 테스트에서 플레이어를 적 근처로 옮겨놓고, 그 뒤 이동 테스트에서 플레이어가 적에게서 멀어진 걸 되돌려놓지 않은 채 투척 사거리를 체크해서 사거리 밖으로 거부된 것—였고 실제 게임 로직 버그는 아니었음.)

## 2026-09-17
- **던전 생성 방식 재논의 및 확정** ([[02_dungeon_town_structure]] "던전 생성 방식" 전체 갱신): 기존 "구조까지 절차적으로(칸/청크 단위) 생성"하는 계획을 재검토 후 폐기. 논의 경로: (1) 사용자가 "층 지형은 고정, 몬스터/함정/상자 위치만 랜덤"을 제안 → (2) 청크(방/복도 조각) 조립형을 중간 대안으로 제시 → (3) 사용자가 스스로 "청크 조립형이 조각 디자인+조립 알고리즘+런타임 내비메시 베이킹까지 겹쳐서 오히려 고정형보다 총 작업량이 더 클 수 있다"는 핵심을 캐치 → (4) 완전 고정(반복 플레이 시 금방 외워져서 재미 죽음)과 완전 절차적(작업량 문제) 둘 다 기각하고 **층별 변형 풀**(층/구간마다 손으로 만든 레이아웃 3~5개, 런마다 무작위 선택 + 그 안에서 몬스터/함정/상자는 랜덤 배치)로 최종 확정.
  - **테마 분기**: 층 사이 포탈 선택에 따라 다음 구간 테마(변형 풀)가 갈라지되, 슬레이 더 스파이어 맵처럼 주기적으로 다시 합류하는 DAG 구조 — 풀 개수가 지수적으로 안 늘게 분기 폭/합류 주기를 제한.
  - **층 스킵 없음**: "고레벨 플레이어도 저층부터 매번 진입해야 해서 루즈해지지 않을까"라는 우려에 대해, 처음엔 "이미 도달한 깊이로 바로 가는 지름길 포탈" 해법을 제안했으나 사용자가 명확히 기각 — 세계관 다이제시스에 안 맞고, 스킵 기준 자체가 모호하며, 무엇보다 "저층 몬스터에게 자원을 낭비하지 않고 고층까지 들고 가는 것이 실력"이라는 게 이미 확정된 하드코어 톤/학습 곡선 철학과 더 잘 맞는다고 판단. 숙련자는 별도 보상 없이도 외운 레이아웃을 빠르게 돌파하게 되고, 방심하면 고층에서 대가를 치르는 것도 의도된 텍스처로 남김.
  - 실제 구현은 착수 전 — 변형 풀 개수/그룹핑, 분기 폭/합류 주기, 스폰 마커/깊이 테이블은 전부 열린 질문으로 남음.
- **저층 돌파 동기부여 확정** ([[02_dungeon_town_structure]] "저층 돌파 동기부여" 참고) — 위 "층 스킵 없음"으로 설계 개입이 필요 없다고 결론 냈던 것을 사용자가 다시 짚음: 스킵은 안 주더라도 고레벨의 저층 돌파 자체에 뭔가 동기부여가 있어야 한다고 판단. 사용자가 예시로 든 "몬스터 종류당 1회 경험치 + 포탈 최초 개방 시 예외" 아이디어(웹소설 출처)는 검토 후 기각 — 이건 아직 설계 전인 레벨업/성장 시스템 전체를 이 보상 하나 때문에 미리 정하게 되는 셈이라 순서가 거꾸로라고 판단. 대신 검토한 "층별 최단 시간 개인 기록 갱신 보상"도 한 차례 확정했다가 사용자가 문제를 제기해 재검토 — 자기 기록과 비교하는 방식은 잘할수록 다음 기준이 어려워져서 "너무 잘해서 못 깰까봐 일부러 포탈 앞에서 시간을 죽인다"는 역설적 행동(플레이 흐름 저해)을 유발할 위험이 있음(스피드런 커뮤니티에 흔한 문제). **최종적으로 디자인이 미리 정한 고정 시간 구간 달성 시 보상**(개인 기록이 아니라 항상 같은 고정 기준과 비교 — "내가 세운 기록을 내가 못 깨는" 문제 자체가 안 생김)으로 확정, 개인 최고 기록은 보상과 무관하게 표시만(성장 체감용, 분리). 시간은 층 번호가 아니라 **레이아웃 변형 단위**로 측정 — 오늘 확정한 층별 변형 풀 구조와 자연스럽게 연결됨. 구간 수치와 보상 종류는 미정. — [[02_dungeon_town_structure]]

- Godot 구현: **"제대로 된" 공격 시스템으로 교체.** 지금까지 공격은 `CombatFormulas.basic_attack_damage()`(힘 스탯만 고정 반영하는 placeholder, 사거리 체크도 별도 `INTERACT_RANGE`로 어중간하게 처리)였는데, 사용자가 이게 "테스트용 기능처럼 느껴져서 만들기가 꺼려진다"고 명확히 말함 — 임시 지형(맵) 작업보다 이걸 먼저 "제대로" 만들기로 결정. 새 `SkillData` 리소스(`scripts/data/SkillData.gd`)로 스킬 = `scaling_stat`×`scaling_multiplier`(데미지) + `range`+`effect_radius`(사거리/효과반경, 08-18에 확정한 스킬 범위 구조 그대로 사용) + `stamina_cost` 구조를 실제로 구현, `CombatFormulas.skill_damage()`가 이걸 계산. 유파/슬롯/숙련도 시스템([[06_skill_style_system]])은 아직 설계가 안 끝나서 이번엔 안 건드림 — 플레이어는 스킬 2개("베기"=E, "강타"=2번 키, 둘 다 힘 스케일링, 데미지·스테미나 비용만 다름)를 그냥 고정 장착, 몬스터도 같은 구조로 `MonsterData.attack_skill` 하나를 가짐. 기존 수치와 연속성 유지(베기 데미지=10, 몬스터 공격 데미지=5로 예전 placeholder와 동일) — 밸런스가 바뀐 게 아니라 "같은 숫자를 내는 진짜 시스템"으로 교체한 것. 헤드리스 스모크 테스트로 사거리 밖 거부, 두 스킬의 서로 다른 데미지/비용, 스테미나 부족 거부, 몬스터 공격도 같은 파이프라인을 타는지까지 검증. — [[03_combat_system]], [[06_skill_style_system]]
- Godot 구현: **공격도 마우스로 조준하도록 변경.** 사용자가 새 스킬 시스템을 실제로 테스트해보고 "스킬 키를 누르면 유일한 적에게 자동 적용돼서 사거리/효과반경을 테스트하기 어렵다"고 지적 — 조준(누구를/어디에) 단계가 통째로 빠져있던 게 문제였음. `Player`에 `pending_skill_index`/`start_aiming_skill()`/`cancel_aim()` 추가 — 스킬 키는 스킬을 "장전"만 하고, 그다음 좌클릭이 실제 목표 지점을 확정(`skill_aim_requested` 시그널, `InventoryPanel.item_throw_requested`와 같은 UI-의도-신호/씬-효과-해석 분리 패턴). 이동 클릭과 조준 클릭은 `pending_skill_index`로 구분해서 같은 좌클릭 핸들러가 상황에 따라 다르게 동작. 우클릭 또는 같은 스킬 키 재입력으로 조준 취소. 조준 지점이 사거리 밖이면 아예 거부(무소모), 사거리 안인데 적과 먼 지점이면 빗나감으로 처리(투척과 같은 정신 — 스테미나는 그래도 소모). 단일 타겟(효과반경 0) 스킬의 "명중" 판정용으로 게임플레이 수치가 아닌 입력 정밀도 보정 상수(`Dungeon.SINGLE_TARGET_AIM_TOLERANCE`)를 별도로 둠 — 효과반경 개념 자체를 흐리지 않기 위함. `MovementRangeIndicator`가 조준 중엔 스킬 사거리 원반(다른 색)을 보여줘서 사거리가 실제로 눈에 보이게 함 — 이게 사용자가 원래 지적한 "테스트하기 어렵다"는 문제의 핵심 해결책. 효과반경 프리뷰는 지금 두 스킬 다 효과반경 0이라 스코프에서 뺌. 헤드리스 스모크 테스트로 장전-즉시발동 안 됨, 같은 키 재입력/우클릭 취소, 사거리 밖 거부, 사거리 안-빗나감, 명중까지 전부 검증. — [[03_combat_system]]
- 테스트용 스킬 사거리를 서로 다르게 조정 — 베기 1.5→2.0, 강타 1.5→1.0(둘 다 원래 같아서 사거리 원반 테스트가 의미 없었음). 겸사겸사 강타가 "더 가까이 들어가야 하는 대가로 더 세게 친다"는 리스크/리워드 성격도 자연스럽게 생김.
- **광역기 무대상 사용 확정** ([[03_combat_system]] "광역기는 대상 없이도 사용 가능"): 사용자가 효과반경>0 스킬을 빈 공간에 조준 가능해야 할지 물음 — 단일 타겟 스킬도 이미 빗나감(스테미나만 소모)을 허용하고 있어서 광역기만 예외로 "대상 필수"로 막으면 시스템 내 규칙이 갈리고, 커밋에는 대가가 따른다는 기존 하드코어 철학과도 안 맞는다고 판단해 **허용**으로 확정. 사전에 유효 대상 존재를 검증하는 구현 복잡도도 얻는 이득에 비해 안 맞는다고 봄. 실제로 `_on_skill_aim_requested()`는 이미 이 방향으로 동작하고 있었음(의도적 확정, 코드 변경 없음).
- **위 결정 정정: 단일 타겟은 빗나감 자체가 없어야 함** ([[03_combat_system]] "단일 타겟과 광역기는 판정 방식이 다름") — 사용자가 바로 다음에 위 결정의 절반("단일 타겟도 적 없는 곳에 쓸 수 있다")을 명확히 기각. 이유: 턴제 게임에서 단일 타겟 스킬에 FPS급 마우스 정밀도를 요구하는 게 과함, 그리고 광역기의 무대상 허용은 애초에 "실수 처벌"이 목적이 아니라 장판/함정처럼 위치 지정 자체가 전략적 용도라서 허용한 것이었음(둘의 허용 근거가 서로 다름을 재확인). 그래서 판정을 분리: **단일 타겟**은 클릭이 실제로 살아있는 적 근처(`SINGLE_TARGET_SELECT_RADIUS`, 관대한 타겟-선택 판정 반경)에 닿아야만 발동하고, 안 닿으면 "빗나감"이 아니라 "유효 대상 없음"으로 아무것도 소모하지 않음. **광역기**만 원래 결정대로 대상 유무와 무관하게 항상 발동. `Dungeon.SINGLE_TARGET_AIM_TOLERANCE` → `SINGLE_TARGET_SELECT_RADIUS`로 이름도 바꿔서 역할을 명확히 함(같은 값, 다른 의미 — "명중 판정 여유"가 아니라 "타겟 선택 판정"). 헤드리스 스모크 테스트로 단일 타겟의 빈 공간 무반응(무소모), 실제 타겟 명중, 광역기의 무대상 발동(소모는 되지만 데미지 없음)까지 검증. — [[03_combat_system]]
- 테스트용 사거리 재조정 — 베기 2.0→3.0, 강타 1.0→5.0(사용자가 둘이 뒤바뀐 것 같다고 지적해서 교환 + 테스트하기 좋게 전체적으로 증가). 리스크/리워드 서사는 뒤집힘(강타가 이제 더 멀리 나감) — 밸런싱 영역이라 나중에 다시 조정 가능.
- Godot 구현: **1층 변형 1(첫 실제 던전 레이아웃) 제작.** 오늘 확정한 "층별 변형 풀" 방향의 1층 첫 변형만 먼저 구현. 테마는 평범한 유적(회색 돌벽+횃불). 사용자가 "방으로 나눌 필요가 있냐"고 지적 — 절차적 생성이 아니라 손으로 만든 고정 변형이라 알고리즘이 연결할 방 구조가 필요 없다는 걸 재확인, 방/복도 대신 기둥·돌더미로 시야만 끊는 **하나로 이어진 열린 공간**으로 방향 전환. `DungeonDressing.gd`가 하드코딩된 좌표(랜덤성 없음)로 벽/기둥/횃불을 런타임에 세움 — `MovementRangeIndicator`처럼 코드로 메시를 짓는 기존 패턴 재사용.
  - 벽을 세운 김에 **`Player`를 `Node3D`→`CharacterBody3D`로 전환**, 자유 이동을 `move_and_slide()` 기반으로 교체(`motion_mode`는 FLOATING — 중력 없는 탑다운) — 이번에 처음으로 실제 충돌체가 생김. 전투 중 클릭 이동(`_move_toward()`)은 의도적으로 그대로 둠(경로탐색 여전히 미착수, 지금은 필요 없음).
  - `ExtractionPoint`를 입구로 재사용해서 던전 진입마다 그 자리로 플레이어 위치를 스냅(`Dungeon._ready()`) — 마을에서 어디 있었든 매 런은 입구에서 새로 시작.
  - **테스트 방법론 추가 발견**: `CharacterBody3D`의 `.position`을 직접 대입하고 바로 다른 노드의 `_process()`에서 그 결과를 확인하면, `await get_tree().process_frame` 한 번만으로는 오래된 상태를 읽을 수 있음(조우 트리거 테스트가 처음에 이렇게 실패함) — `await get_tree().physics_frame`을 먼저 끼워서 물리 서버 동기화를 기다리면 해결됨. 메모리에 기록해둠.
  - 헤드리스 스모크 테스트로 입구 스냅, 지오메트리 생성 개수, 실제 충돌(벽을 뚫고 지나가지 않는지), 새 위치에서 조우/추출 트리거 정상 동작까지 검증. — [[02_dungeon_town_structure]]

## 2026-09-19
- 테스트 플레이 결과 1층에서 **벽 뒤 적을 클릭해도 못 때리는 문제** 발견 → 수정. 진단: 코드에 벽 클릭 판정은 없었고, 단일 타겟 조준이 "클릭한 바닥 지점이 적 반경 1.0 안인가"였는데 카메라가 25°로 비스듬해서 적 몸통/앞을 가린 벽을 클릭하면 광선이 적 **뒤쪽 바닥**에 닿는 게 원인(추정 — 코드와 카메라 각도로 계산, 실제 클릭 재현은 안 해봄). 수정: `Monster`에 스프라이트 크기의 클릭용 충돌체(전용 물리 레이어 2, 이동을 막지 않음, 죽으면 비활성)를 달고, `Targeting.pick_enemy()`가 카메라 광선을 그 레이어에만 쏴서 적을 직접 판정. 벽은 레이어 1이라 클릭을 못 가로챔. `Player.skill_aim_requested` 시그널에 `picked` 인자 추가, 단일 타겟 사거리는 클릭 바닥 지점이 아니라 **선택된 적까지의 거리**로 계산, 광역기는 기존대로 바닥 지점 사용. 기존 `SINGLE_TARGET_SELECT_RADIUS` 상수는 삭제. 헤드리스 스모크 테스트로 적 화면 위치 클릭 시 선택됨/빈 공간은 null/사이에 벽을 세워도 선택됨/선택 없으면 무소모/사거리 밖 거부/조준→클릭 전체 경로 명중까지 검증. — [[03_combat_system]]
  - **알려진 부작용**: 엄폐/시야 판정이 없어서 이제 사거리만 되면 벽 너머 적도 때릴 수 있음. 사용자가 이걸 어떻게 처리할지 고민해보겠다고 함 — 결정 전까지 그대로 둠(엄폐 자체는 08-09에 "장애물이 시야/공격을 막음"으로 방향만 확정된 상태).
  - 같이 논의된 것(미착수): 카메라와 플레이어 사이의 벽을 반투명 처리하는 기능 — 광선으로 가리는 메시를 찾아 페이드하는 방식이라 맵 구조와 무관하게 재사용 가능하지만, GL Compatibility 렌더러에서 인스턴스 `transparency`가 먹는지 확인 필요(안 되면 벽마다 머티리얼 분리, 지금은 공유 중). 사용자가 1층 정식 설계 때 함께 다루기로 함.
- **고저차 사거리 보정 + 사거리 표시 방향 확정(설계만, 미구현)** ([[03_combat_system]] "고저차 사거리 보정과 사거리 영역 표시"): 사용자가 "고저차 있는 시스템으로 기획했는데 Y를 안 보고 있다"고 지적 — 기획상 XZ 거리 + 고저차 별도 보정이었는데 보정 쪽이 통째로 미구현이었음(지형에 높낮이가 없어서). 사용자는 높은 곳에서 **사거리 부스트**를 원함 → 기획의 "사거리 또는 명중률" 중 사거리로 확정, 식은 `유효 사거리 ≈ 스킬 사거리 + (내 높이 − 대상 높이) × 계수`(아래 적은 늘고 위 적은 줄고 같은 높이는 그대로). 표시 방식으로 (1) 적에 마우스를 올리면 결과/±태그를 보여주는 안, (2) 원반 안팎 점선 링, (3) 지형을 따라가는 사거리 영역을 검토 → 사용자가 1번은 "왜 안 되는지/되는지 모른다"는 이유로, 2번은 지저분하다는 이유로 기각하고 **3번 확정**(때릴 수 있는 바닥 영역 자체를 그림 — 낮은 쪽 불룩/높은 쪽 움푹, 표시와 판정이 같은 계산, 나중에 엄폐로 가려진 지점도 같은 영역에서 빼면 됨). 사용자가 계단식 경계가 아니라 자연스럽게 이어지게 할 수 있냐고 물어서, 높이를 내부적으로 연속값(float)으로 다루고 3단계는 디자인 눈금으로만 쓰는 방향을 메모(진짜 절벽은 판정은 정확히, 표시만 살짝 페이드). 구현은 1층에 실제 높낮이를 넣을 때.
- **맵 데이터를 그리드로 저장할지 논의 → 그리드 불필요, 플레이어 쪽 고저차 기반 시험 구현** ([[02_dungeon_town_structure]] "고저차 시험"): 사용자가 고저차를 고려한 맵 설계에서 그리드 저장이 필요한지, 삼각형 그리드가 더 자연스러운지, 데이터량이 너무 많지 않은지 물음. 결론: 이동/조준/사거리를 이미 연속 좌표로 하고 있어서 게임플레이에 격자 데이터는 필요 없고, 높이는 실제 지오메트리에 아래로 광선을 쏴서 읽음(연속 높이, 경사로도 그대로 처리). 삼각형/육각형 격자는 칸 단위 이동용 이점이라 자유 이동인 우리에겐 무의미하고, 3D 지형은 원래 삼각형 메시. 데이터량은 24×24 공간을 0.25 간격 높이맵으로 잡아도 수백 KB 이하라 문제 아님(부담은 데이터가 아니라 제작 노력). 사용자가 "지형을 값에 맞춰 변형시키고 타입별 텍스처를 붙이는" 값 기반 제작 방식이 아니냐고 되물음 → 그건 **제작 방식**의 하나(배치형 블록 vs 값 기반)이고 제가 말한 건 **높이를 읽는 방식**이라 충돌하지 않음을 설명, 실제 1층을 어느 쪽으로 만들지는 미정으로 남기고 이번 시험은 가장 빠른 배치형(발판+경사로 블록)으로 진행.
  - 구현: `Player` GROUNDED+중력(20.0, 임시값), 전투 모드에서도 중력이 바닥에 붙임. 물리 레이어 3을 "바닥" 전용으로 신설(`Targeting.GROUND_LAYER`) — 바닥/발판/경사로는 레이어 1+3이라 플레이어와 충돌하고, 높이/클릭 광선은 레이어 3만 봐서 벽에 안 걸림. `Targeting.ground_height()`(높이 읽기)와 `raycast_to_ground()`(실제 바닥 지오메트리 기준 클릭 지점 — 기존 "평평한 평면" 가정은 발판 위에서 ~2.1 유닛 어긋남을 테스트로 확인) 추가. 전투 클릭 이동은 도착 지점 바닥 높이로 y를 직접 맞춤. 마을 바닥에 충돌체 추가(없으면 중력 때문에 떨어짐). 사거리 원반이 플레이어 높이를 따라감. 던전 공간에 발판(높이 1.0)+경사로 임시 추가.
  - **알려진 한계**: 마을/던전 가장자리 밖으로 걸어 나가면 끝없이 떨어짐(낙사/가장자리 처리 없음). 등반 스테미나 추가 소모, 고저차 사거리 보정 자체는 미구현.
  - 헤드리스 스모크 테스트로 마을/던전 바닥 안착, 높이 읽기(0.0/1.0/≈0.5), 경사로 오르기, 발판에서 낙하 착지, 발판 위 바닥 클릭 정확도, 전투 이동 후 높이 스냅까지 검증.
- **전투 이동을 순간이동에서 내비메시 경로 걷기로 교체** ([[03_combat_system]] "이동 방식"): 사용자가 테스트 후 "전투 중 이동이 순간이동이라 어색하다"고 지적. 짚어보니 더 큰 문제는 순간이동이 벽/기둥을 그냥 통과한다는 것(1층에 벽이 생기기 전엔 티가 안 났음) — 지난번에 "장애물이 없어서" 미뤄둔 경로탐색이 필요해진 시점. 사용자가 규칙을 직접 정함: 이동 가능 영역 표시는 불필요 / 벽을 누르거나 스테미나 밖을 눌러도 허용 범위까지는 이동 / 경로가 연결되지 않으면 아예 이동 안 함 / 걷는 도중 다른 입력은 막되 **클릭으로 중간에 멈출 수 있게**(걷는 걸 보다가 스테미나를 보고 멈추기 위함).
  - 구현: `DungeonDressing._bake_navigation()`이 지오메트리를 세운 직후 내비메시를 한 번 굽고(레이어 1 정적 충돌체만, 플레이어 캡슐과 같은 반경 0.4), `Player.start_walk()`가 `NavigationServer3D`로 목표를 가장 가까운 걸을 수 있는 지점으로 보정 → 경로 계산 → 경로 끝이 목표에서 떨어져 있으면(연결 안 됨) 무시 → 스테미나 예산만큼 경로를 자름. 걷기는 물리 스텝에서 `move_and_slide()`로 진행하며 **실제로 걸은 거리만큼 스테미나를 차감**(멈추면 걸은 만큼만 소모). 걷는 동안 `Dungeon`의 스킬 장전/턴 종료/되돌리기/투척이 `is_moving`으로 막히고, 클릭하면 `stop_walking()`. 기존 `_move_toward()`(순간이동)는 제거. 막힘 방지용으로 0.5초 이상 거의 못 움직이면 걷기를 포기.
  - 헤드리스 스모크 테스트로 열린 바닥 걷기(4유닛 = 스테미나 20), 기둥 우회(직선 30보다 비쌈), 스테미나 부족 시 딱 허용 거리까지(25 → 5유닛), 벽 클릭 시 벽 앞까지, 연결 안 된 섬 목표는 무이동·무소모, 스테미나 0이면 시작 안 함, 클릭으로 중간 정지(걸은 만큼만 소모), 걷는 중 입력 차단, 경사로 → 발판 걷기까지 검증. 테스트 중 발견: 얇은 벽(두께 1) 한가운데를 누르면 양쪽 면까지 거리가 같아서 반대편으로 돌아가 도착할 수 있음(동작 자체는 정상, 테스트 목표를 안쪽 면 쪽으로 옮김).
  - **작업 중 실수 공개**: 중간에 빈 `python -` 명령이 stdin을 기다리며 멈춘 걸 정리하려고 "python"이 들어간 프로세스를 종료했는데, 무엇을 죽이는지 확인하지 않고 4개를 종료함(대상이 아닐 수 있는 에디터 보조 프로세스 포함 가능). 코드/저장소에는 영향 없음.
- **걷기 이동 테스트 후속 수정 3건** ([[03_combat_system]] "이동 방식"): 사용자가 직접 테스트하고 지적.
  1. **이동 반경 원반이 안 사라짐** → 제거. 내가 "그대로 둘까요?"라고 물어보고 남겨뒀는데, 사용자는 "표시할 필요 없다"고 이미 말한 것이라 물어볼 게 아니라 지웠어야 했음. 스킬 조준 중의 스킬 사거리 원반은 유지(`MovementRangeIndicator`는 이제 스킬 사거리 전용, 이름은 `Dungeon.tscn` 수정을 피하려고 그대로).
  2. **경사로를 옆에서 올라가려 하면 옆면에 박혀 멈춤** → 재현 후 수정. 재현 테스트에서 동쪽/서쪽 옆에서 출발하면 경사로 옆면(x≈2.1)까지 걸어가서 멈추는 걸 확인. 원인은 내비메시와 물리의 불일치 — 플레이어는 턱을 못 넘는데 내비메시(기본 `cell_height`/`agent_max_climb` 0.25)는 경사로 옆면의 낮은 부분을 계단으로 잡아 옆 진입 경로를 만듦. `cell_height` 0.05 + `agent_max_climb` 0.1(각각 셀 크기의 정수배로 맞춰 정밀도 경고도 제거)로 해결, 옆에서 눌러도 앞으로 돌아 올라감. 재현 테스트 5건(정면/동쪽 옆/서쪽 옆 경사로, 동쪽에서 발판, 경사로 내려가기)이 전부 통과. 부수 효과로 옆에서 출발하면 돌아가는 경로가 길어져 스테미나가 더 듦.
  3. **높은 곳에서 아래를 누르면 뛰어내리지 않고 경사로로 내려옴** → 버그가 아니라 내비메시에 절벽 연결이 없어서 생기는 자연스러운 결과라고 설명, 그대로 둠. 뛰어내리기/점프는 별도 행동으로 나중에 결정(열린 질문으로 기록) — 고지대에 올라가는 길이 경사로뿐이어야 고지대 사거리 보너스가 가치를 가지므로 점프 업은 특히 신중히.

## 관련 문서
- [[00_game_overview]]
